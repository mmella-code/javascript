let marks = 50;
let res = (marks > 39) ? "pass": "fail";
console.log(res);

// nested ternary operator

let day = 1;
let greeting = (day === 1)? 'start of the week': 
(day === 2)? 'second day':
console.log(greeting);
  
           
